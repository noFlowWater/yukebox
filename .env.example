# Server port (only port you need to expose)
PORT=3000
NODE_ENV=production

# PulseAudio socket and cookie (auto-detected by setup.sh)
PULSE_SOCKET=/run/user/1000/pulse/native
PULSE_COOKIE=/home/user/.config/pulse/cookie

# Auth secret (auto-generated by setup.sh)
JWT_SECRET=

# Public URL for OG image / social sharing (set to your actual domain)
# NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# Advanced / optional
# BACKEND_PORT=4000
# MPV_SOCKET=/tmp/mpv-socket
# DB_PATH=/app/data/yukebox.db
